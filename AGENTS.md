# Project Agent Rules

## 闭环测试规则（由 closed-loop-test 自动生成）

### 状态

- 闭环测试：已启用
- 测试方案：`docs/test-plan.md`（判定标准已锁定）
- 过程日志：`docs/dev-log.md`
- P1 前端验证方案：Vitest 组件/行为测试；生产页面使用只读 HTTP 探针。当前工作区未安装 Playwright，涉及真实剪贴板权限的浏览器交互保留人工抽查

### 编码纪律

1. 每完成一个逻辑单元，运行 `docs/test-plan.md` 中对应的 P0/P1 验证。
2. 后端用 Node 测试和 HTTP 探针；前端用 Vitest；CLI 用隔离的临时 HOME/数据目录测试。
3. 每完成三条功能路径，重跑全部 P0 和已通过的 P1。
4. 判定标准不能为了通过而放宽；验证命令可因本机运行时路径调整，但必须记录在 `docs/dev-log.md`。
5. 每轮验证把实际命令、结果、失败原因和修复追加到 `docs/dev-log.md`。

### 失败停止条件

| 条件 | 响应 |
|---|---|
| 单项失败达到 5 次 | 停止该项并报告全部尝试 |
| 修 A 破 B 循环达到 2 次 | 停止并重新检查架构 |
| 总修复次数达到 15 次 | 停止并生成失败报告 |
| P0 连续 3 轮无法全过 | 停止并优先排查环境 |

### 交付标准

P0 与 P1 全部通过，在 `docs/dev-log.md` 写最终摘要，完成独立代码审查，GitHub 检查全绿，再部署生产和执行线上探针。
