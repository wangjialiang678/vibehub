# VibeHub 项目约定

## 先读这些，别重新猜

| 你要做什么 | 先读 |
|---|---|
| 任何涉及服务器 / 域名 / 备案 / 端口的判断 | `docs/research/infra-facts.md` — 里面都是**实测过的事实**，不要重新推测 |
| 任何"这个要不要自己写"的判断 | `docs/research/codebase/existing-assets.md` |
| 改数据结构 / 状态流转 | `docs/specs/domain-model.md` |
| 改接口 | `docs/specs/api.md` |
| 判断某功能是不是 P0 | `docs/specs/PRD.md` §3 范围裁剪标准 |

## 这个项目的硬约束

1. **学员代码不可信。** 它是 AI 生成、没人逐行看过的。服务端**不执行**学员的构建脚本、不跑学员的后端进程。
2. **作品里没有秘密。** 作品是公开静态页，任何写进产物的东西都等于公开。保护来自服务端策略，不来自"把 key 藏在前端"。
3. **版本不可变。** 落盘后不再修改，审核/部署/诊断结果都挂关联表。
4. **老师审核是发布的唯一闸门。** AI 诊断不得自动放行。
5. **诊断分数由确定性检查器算，模型只翻译。** 模型不得修改 points / blocker / evidence_level，每条结论必须引用 check_key。
6. **不适用的诊断项不进分母。** 纯展示型作品的"服务端"维度显示"不适用"，不是 0 分。
7. **面向学员的错误一律中文人话 + 下一步提示。**
8. **跨权限边界的请求返回 404 而不是 403。** 不泄露"存在但无权"。

## 不要做的事

- ❌ 不要引入 UI 组件库（antd / MUI）——会毁掉原型的手写 CSS 质感。设计 token 见 `docs/research/prototype/tokens.json`
- ❌ 不要引入 k8s / 微服务 / 消息队列——2 核机器 + 1 名主力工程师
- ❌ 不要把运行时数据（数据库、版本产物、上传文件）提交进仓库
- ❌ 不要在没有 `applicability` 的情况下算诊断百分比
- ❌ 不要用 `--no-sandbox` 跑 Chromium

## 配色语义（需求文档 §9.1.4，必须遵守）

绿 `--success` = 已可用 · 蓝 `--blue` = 正在完善/已连接 · 琥珀 `--amber` = 需要关注 · 珊瑚红 `--coral` = **仅**用于高优先级问题或关键数字

## 决策记录

产品与架构决策走 Vibe Workbench 会话 `vibehub`。未决项列在 `docs/specs/PRD.md` §8 开放问题。**遇到标注〔决策 N〕的地方，先查该表确认是否已有结论，没有就问，不要自己定。**

## 文档同步

改了架构或接口后，同步更新 `docs/specs/` 下对应文档。事实类结论（服务器状态、备案、容量）改动要回填 `docs/research/infra-facts.md` 并注明核查方式。
